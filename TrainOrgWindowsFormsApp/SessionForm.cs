using System;
using System.Windows.Forms; 

namespace TrainOrgWindowsFormsApp
{
    public partial class SessionForm : Form
    {
        public SessionForm()
        {
            InitializeComponent();
            
            // Создаем экземпляр класса Person
            //Session session = new Session();
            //{
            //    Name = "John Doe",
            //    Age = 30,
            //    Gender = "Male"
            //};

            //// Выводим свойства на форму
            //lblName.Text = "Name: " + person.Name; // lblName - Label для отображения имени
            //lblAge.Text = "Age: " + person.Age.ToString(); // lblAge - Label для отображения возраста
            //lblGender.Text = "Gender: " + person.Gender; // lblGender - Label для отображения пола
        }

    }
}
